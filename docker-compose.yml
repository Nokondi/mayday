services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mayday
      POSTGRES_PASSWORD: mayday_dev
      POSTGRES_DB: mayday
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
